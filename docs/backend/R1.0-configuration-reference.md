# Satori R1.0 配置参考

> 适用范围：API、Worker、迁移、Seed、测试环境和生产环境。
>
> 原则：环境变量只表达部署差异、外部依赖、密钥和容量；非敏感业务、安全与可靠性规则进入版本化策略；运行服务不提供 Mock/Stub 切换。

## 1. 配置来源与优先级

1. `backend/docker-compose.test.yml` 为测试环境强制注入 `HOST`、`PORT`、`DATABASE_URL`、`REDIS_URL`、`COOKIE_SECURE` 和 `SMS_DELIVERY_MODE=FIXED_CODE`，优先级高于 `.env.production`。
2. 其余部署变量由服务器专用 `.env.production` 注入，完整定义、允许值和影响见 `backend/.env.example`。
3. 产品、安全和可靠性规则来自 `backend/packages/infrastructure/src/config/runtime-policy.ts`，只能通过代码评审和发版修改。

应用启动时由 Zod Schema 校验部署变量。未知的历史变量不会改变运行策略，但发布时仍应从服务器文件移除，避免误导运维人员。

## 2. 保留的部署环境变量

| 类别          | 配置项                                                                                    | 影响范围                                |
| ------------- | ----------------------------------------------------------------------------------------- | --------------------------------------- |
| 进程与网络    | `HOST`、`PORT`                                                                            | 监听地址和端口映射                      |
| 数据基础设施  | `DATABASE_URL`、`DATABASE_POOL_MAX`、`REDIS_URL`                                          | PostgreSQL、Redis、API 和 Worker 可用性 |
| 队列容量      | `QUEUE_PREFIX`、`QUEUE_CONCURRENCY`、`QUEUE_JOB_TIMEOUT_MS`                               | 环境隔离、任务并发、超时与失联恢复      |
| HTTP 与安全   | `CORS_ORIGINS`、`COOKIE_SECURE`                                                           | H5 跨域和 Refresh Cookie 传输           |
| 应用密钥      | `CURSOR_SIGNING_SECRET`、`AUTH_HMAC_SECRET`、`ACCESS_TOKEN_SECRET`、`DATA_ENCRYPTION_KEY` | 游标、身份哈希、Token 和字段解密        |
| 短信供应商    | `SMS_DELIVERY_MODE`、`SMS_GATEWAY_URL`、`SMS_GATEWAY_API_KEY`、`SMS_GATEWAY_TIMEOUT_MS`   | 固定测试码或真实短信发送                |
| Aqua 基础连接 | `AQUA_BASE_URL`、`AQUA_SERVICE_KEY`                                                       | 同一 Aqua 租户及其全部 Workflow         |

`backend/.env.example` 对上表每个变量都使用固定格式说明：

```text
# 用途：为什么需要该变量
# 取值：允许值、格式或范围
# 影响：修改后会影响哪些服务或业务流程
KEY=value
```

配置单测会逐项核对 Schema 变量是否具有这三类注释；漏写说明会使测试失败。

四卡初识报告不是部署选项：所有环境固定使用本地确定性知识生成器，不存在启用开关、LOCAL/Aqua 选择器或环境专用超时配置。

完整每日指引与首页能量摘要属于同一 Aqua 租户，并由同一个 Service Key 调用租户下的不同 Workflow。两项连接配置由 `AquaClientFactory` 统一读取；业务模块不得再自行配置 Base URL 或 Service Key。Workflow、超时、重试和预热参数是代码策略，不是服务器差异。

## 3. 版本化 R1 策略

| 策略路径                                     |                            当前值 | 用途与影响                                           |
| -------------------------------------------- | --------------------------------: | ---------------------------------------------------- |
| `auth.accessTokenTtlSeconds`                 |                           1800 秒 | 新签发 Access Token 的有效期                         |
| `auth.refreshTokenTtlSeconds`                |                        2592000 秒 | 新 Session、Refresh Token 和 Cookie 的有效期         |
| `auth.otpTtlSeconds`                         |                            300 秒 | 新验证码挑战有效期                                   |
| `auth.otpResendSeconds`                      |                             60 秒 | 验证码再次发送等待时间                               |
| `auth.otpMaxAttempts`                        |                              5 次 | 单挑战最大错误尝试次数                               |
| `auth.rateLimitsPerHour.phone`               |                              5 次 | 单手机号每小时申请验证码上限                         |
| `auth.rateLimitsPerHour.device`              |                             10 次 | 单设备每小时申请验证码上限                           |
| `auth.rateLimitsPerHour.ip`                  |                             20 次 | 单 IP 每小时申请验证码上限                           |
| `idempotency.ttlSeconds`                     |                         604800 秒 | 登录、档案、生成、反馈和注销等写操作的幂等结果保留期 |
| `queue.maxAttempts`                          |                              5 次 | BullMQ 与数据库生成任务的默认尝试预算                |
| `queue.backoffMs`                            |                         2000 毫秒 | 队列和 Outbox 指数退避基础值                         |
| `aqua.dailyInsight.workflowId`               |                   `daily-insight` | 完整每日指引固定调用的 Workflow                      |
| `aqua.homeEnergySummary.workflowId`          |       `daily-energy-home-summary` | 首页摘要固定调用的 Workflow                          |
| `aqua.homeEnergySummary.workflowVersion`     | `daily-energy-home-summary/1.0.3` | 首页缓存隔离与 Workflow 版本                         |
| `aqua.homeEnergySummary.requestTimeoutMs`    |                        15000 毫秒 | 单次首页摘要 Aqua 请求超时                           |
| `aqua.homeEnergySummary.maxAttempts`         |                              2 次 | 首页摘要可重试错误的最大尝试次数                     |
| `aqua.homeEnergySummary.retryBackoffMs`      |                          250 毫秒 | 首页摘要重试基础退避时间                             |
| `aqua.homeEnergySummary.prewarm.days`        |                              3 天 | 每轮预热覆盖今天及未来日期                           |
| `aqua.homeEnergySummary.prewarm.concurrency` |                                 3 | 同时执行的预热请求数                                 |
| `aqua.homeEnergySummary.prewarm.spacingMs`   |                         5000 毫秒 | 相邻预热请求的最小间隔                               |
| `aqua.homeEnergySummary.prewarm.intervalMs`  |                      3600000 毫秒 | Worker 补齐缓存的执行周期                            |
| `profile.previewTtlSeconds`                  |                          86400 秒 | 档案预览有效期                                       |
| `profile.firstLookStaleAfterMs`              |                       330000 毫秒 | 四卡初识生成记录的失联接管时间                       |
| `dailyInsight.historyDays`                   |                             90 天 | 每日指引历史查询窗口                                 |
| `dailyInsight.price`                         |                              1 颗 | 每日指引预留、消费、释放、退款和接口显示的统一金额   |
| `registration.rewardAmount`                  |                             18 颗 | 新用户创建的待领取奖励金额                           |
| `accountDeletion.cancellationHours`          |                          168 小时 | 新注销申请的可撤销冷静期                             |

修改策略时必须同步更新策略版本、单元测试、交接文档和测试环境验证记录。`dailyInsight.price` 在数据库支持价格快照前不得直接改值。

## 4. 首页摘要固定执行策略

首页 HTTP 请求只读共享缓存，不同步调用 Aqua。Worker 固定在启动时和每小时补齐今天及未来两天的六十甲子组合，采用并发 3、发车间隔 5 秒的受控负载。该逻辑是首页摘要成立的必要运行路径，不提供环境开关或服务器档位；调整时应修改版本化策略、补充测试并正常发版。

## 5. 已移除或迁移的历史变量

| 历史变量                                                                                                                                                          | 处理结果                                                              |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `DAILY_INSIGHT_PRICE`                                                                                                                                             | 移入版本化策略，并统一接入预留、消费、释放、退款和接口显示            |
| `FEATURE_LIFE_PROFILE`、`FEATURE_PROFILE_LIBRARY`、`FEATURE_WISDOM_SEEDS`、`FEATURE_DAILY_INSIGHT`                                                                | 删除；旧实现只改变 Bootstrap 响应，H5 和后端权限均未消费              |
| `QUEUE_MAX_ATTEMPTS`、`QUEUE_BACKOFF_MS`                                                                                                                          | 移入版本化队列策略                                                    |
| `ACCESS_TOKEN_TTL_SECONDS`、`REFRESH_TOKEN_TTL_SECONDS`、`OTP_*`、`SMS_*_RATE_PER_HOUR`                                                                           | 移入版本化认证策略                                                    |
| `IDEMPOTENCY_TTL_SECONDS`                                                                                                                                         | 移入版本化幂等策略                                                    |
| `PROFILE_PREVIEW_TTL_SECONDS`、`DAILY_INSIGHT_HISTORY_DAYS`                                                                                                       | 移入版本化业务策略                                                    |
| `REGISTRATION_REWARD_AMOUNT`、`ACCOUNT_DELETION_CANCELLATION_HOURS`                                                                                               | 移入版本化产品策略                                                    |
| `AQUA_AI_BASE_URL`、`AQUA_AI_SERVICE_KEY`、`AQUA_TENANT_SERVICE_KEY`                                                                                              | 合并为唯一连接配置 `AQUA_BASE_URL`、`AQUA_SERVICE_KEY`                |
| `AQUA_AI_WORKFLOW_ID`、`AQUA_AI_WORKFLOW_VERSION`                                                                                                                 | 移入版本化 Aqua 策略；每日指引使用固定 ID 和当前激活版本              |
| `HOME_ENERGY_SUMMARY_ENABLED`、`HOME_ENERGY_PREWARM_ENABLED`                                                                                                      | 删除；首页摘要和 Worker 预热是固定运行路径                            |
| `HOME_ENERGY_SUMMARY_TIMEOUT_MS`、`HOME_ENERGY_SUMMARY_MAX_ATTEMPTS`、`HOME_ENERGY_SUMMARY_RETRY_BACKOFF_MS`                                                      | 移入版本化 Aqua 策略                                                  |
| `HOME_ENERGY_PREWARM_PROFILE`、`HOME_ENERGY_PREWARM_DAYS`、`HOME_ENERGY_PREWARM_CONCURRENCY`、`HOME_ENERGY_PREWARM_SPACING_MS`、`HOME_ENERGY_PREWARM_INTERVAL_MS` | 移入版本化 Aqua 预热策略                                              |
| `NODE_ENV`                                                                                                                                                        | 从 Satori 配置中删除；短信行为改由语义明确的 `SMS_DELIVERY_MODE` 控制 |
| `DAILY_INSIGHT_GENERATOR`、`DAILY_INSIGHT_STUB_MODE`、`DAILY_INSIGHT_STUB_DELAY_MS`                                                                               | 删除；运行服务固定使用 Aqua，每日指引失败路径由测试代码注入依赖验证   |
| `PROFILE_FIRST_LOOK_ENABLED`、`PROFILE_FIRST_LOOK_GENERATOR`、`PROFILE_FIRST_LOOK_TIMEOUT_MS`                                                                     | 删除；四卡初识固定使用本地确定性生成器，失联时间进入版本化策略        |
| `LOAD_TEST_*`                                                                                                                                                     | 删除；仓库不再携带服务器压测脚本或压测环境配置                        |

## 6. 发布检查

发布前执行：

```bash
npm run typecheck
npm run lint
npm run test:contract
npm run test:unit
npm run build
```

部署测试服务器前，先备份服务器 `.env.production`，将两个历史 Service Key 名称合并为 `AQUA_SERVICE_KEY`，删除上表已迁移的历史键，再执行 Compose。不得输出或复制任何密钥值。
