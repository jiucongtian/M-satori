# R1.0 测试环境部署

- 服务器：`101.43.26.35`
- API 域名：`https://test-satori.shenxinyou.com`
- Base URL：`https://test-satori.shenxinyou.com/api/v1`
- 部署目录：`/opt/satori-test/current`
- 前端目录：`/opt/satori-test/frontend/current`
- Compose：`backend/docker-compose.test.yml`
- Nginx：`deploy/nginx/test-satori.shenxinyou.com.conf`
- 当前后端版本：`aca2cdf`
- 部署状态：API、Worker、PostgreSQL、Redis 均健康；直连与 Nginx HTTPS 健康检查通过
- DNS 状态：已生效，公网 HTTPS 可访问
- 前端版本：`1f99118`
- 当前前端发布目录：`/opt/satori-test/frontend/releases/1f99118`
- 前端回滚目录：`/opt/satori-test/frontend/releases/9bec7df`
- 后端回滚目录：`/opt/satori-test/releases/d373eaa`
- PROFILE-11：测试环境显式启用 `PROFILE_FIRST_LOOK_ENABLED=true` 与 `PROFILE_FIRST_LOOK_GENERATOR=LOCAL`
- 完整每日指引：测试环境显式启用 `DAILY_INSIGHT_GENERATOR=AQUA`，Workflow ID 为 `daily-insight`，使用 Aqua 当前激活版本
- Aqua 传输：完整每日指引与首页能量摘要的 Base URL 均使用 HTTPS

## 发布

服务器端 `.env.production` 不进入 Git。发布时同步代码后执行：

```bash
cd /opt/satori-test/current/backend
docker compose --env-file .env.production -f docker-compose.test.yml up -d --build --wait
```

迁移由一次性 `migrate` 服务先执行；成功后 API 和 Worker 才启动。PostgreSQL/Redis 不暴露公网端口，API 仅绑定 `127.0.0.1:3200`，由 Nginx 提供 HTTPS。

前端使用 `npm run build:static` 导出到 `frontend/out/`，同步至版本化发布目录后切换 `/opt/satori-test/frontend/current` 软链接。Nginx 根路径提供前端静态页面，`/api/v1/` 保持反向代理后端。

2026-08-19 的 PROFILE-11 本地确定性生成发布已完成：后端迁移、seed、API 与 Worker 启动成功，前后端不可变目录及软链接均切换至 `639f119`；`nginx -t` 与 reload 成功，公网首页和 `/api/v1/app/bootstrap` 均返回 HTTP 200。

同日配置复核发现，`639f119` 首次部署后的服务器专用 `.env.production` 仅保留 12 个显式键，相比上一发布 `700c2e8` 丢失 17 个键，导致完整每日指引回落至默认 `STUB`，首页 Aqua 摘要与预热也被默认关闭。现已备份原配置，从 `700c2e8` 恢复全部缺失键，保留新增的 `PROFILE_FIRST_LOOK_GENERATOR=LOCAL`，并把两个 Aqua Base URL 从 HTTP 升级为 HTTPS。恢复后 `.env.production` 共 29 个显式键；API、Worker、PostgreSQL、Redis 健康，loopback ready、公网首页和 bootstrap 均验证通过。后续发布必须完整继承服务器专用环境文件，不得从最小模板重新创建。

2026-08-25 已部署配置收敛版本 `aca2cdf`。部署前确认远端 `main` 最新提交与测试环境旧发布基线均为 `d373eaa`；服务器采用不可变发布目录，没有可执行 `git pull` 的工作树，因此从该基线向新目录发布。现有 `.env.production` 已先备份，再按配置名迁移：删除 6 个已废弃且实际存在的旧键，增加 `HOME_ENERGY_PREWARM_PROFILE=NORMAL`，最终保留 25 个服务器显式键，密钥值未输出或写入仓库。通用运行环境 Schema 从 59 项收敛为 35 项；稳定业务规则移入版本化策略 `r1.0-2026-08-25.1`。完整说明见 `docs/backend/R1.0-configuration-reference.md`。

发布后 API、Worker、PostgreSQL、Redis 均正常；loopback ready、公网首页和 bootstrap 均返回 HTTP 200，bootstrap 返回配置版本 `r1.0-2026-08-25.1`，四项 R1.0 产品能力全部启用。旧配置键计数为 0，预热档位键计数为 1。回滚时将 `/opt/satori-test/current` 原子切回 `/opt/satori-test/releases/d373eaa`，并恢复对应的服务器配置备份后重建 API/Worker；本次没有数据库结构变更。

## 验证与回滚

```bash
curl -fsS http://127.0.0.1:3200/api/v1/health/ready
docker compose --env-file .env.production -f docker-compose.test.yml ps
docker compose --env-file .env.production -f docker-compose.test.yml logs --tail=200 api worker migrate
```

应用回滚使用上一 Git 提交重新构建 API/Worker。数据库迁移不得盲目降级；按 Runbook 验证 consent、Session、注销和 seed ledger 事实后再切换流量。

测试环境 `NODE_ENV=test` 使用固定验证码 `123456`。该行为禁止用于正式生产环境。

## 前端联调参数

- Base URL：`https://test-satori.shenxinyou.com/api/v1`
- Credential：Refresh Token 仅通过 `HttpOnly; Secure; SameSite=Lax` Cookie 传递；前端请求需启用 credentials
- CORS：当前允许 `https://test-satori.shenxinyou.com` 和 `http://localhost:3001`；若前端使用其他 Origin，部署前须加入明确白名单
- 联调账号：`+86 18800000000`
- 固定验证码：`123456`
- 设备标识：建议使用 `frontend-handoff-r1`，时区 `Asia/Shanghai`
- 初始数据：账号已注册并接受 R1.0 三份协议，尚未创建档案；可从 `CREATE_PROFILE` 开始验证完整路径
- 页面状态：R1.0 登录、Session 恢复、地点搜索、本人档案 preview/confirm、注册赠礼、种子余额/流水、每日指引生成/轮询/查询、首页，以及人物档案创建/preview/confirm/列表/删除已切换到真实 API
- 浏览器联调：已使用 `18800000001` 完成新用户成功链路，赠礼后余额 3，生成每日指引后余额 2；刷新页面可通过 Refresh Cookie 恢复到首页
- 已知契约缺口：本人档案 preview/confirm 当前不接收用户填写的称呼，服务端固定返回“我的生命智慧档案”；需后续补充称呼写入契约后再移除该限制
- 成功路径：使用固定验证码登录，按 bootstrap 返回的 `requiredLegalDocuments` 和 `nextAction` 驱动页面
- 可复现失败：错误验证码可使用 `000000`；重复命令复用同一 `Idempotency-Key`，冲突测试使用相同 key 加不同 payload
- 生成失败：`DAILY_INSIGHT_STUB_MODE=FAILURE` 仅作为测试进程变量使用，不写入通用 `.env.production`；任务按重试/释放规则终态失败。生产环境不得启用。
- 生成超时：测试进程临时使用 `DAILY_INSIGHT_STUB_MODE=DELAY`，并令 `DAILY_INSIGHT_STUB_DELAY_MS > QUEUE_JOB_TIMEOUT_MS`；可验证 `GENERATION_TIMEOUT`、重试及轮询降级。变量说明见 `backend/test-environment.example`，生产环境不得启用。
- 接口负责人：Backend；前端负责人姓名和联调窗口待项目负责人指定
