# R1.1 商业交易与权益接口交付状态

## 已实现

- 商品：`GET /service-offerings`、`GET /service-offerings/{id}`、`GET /membership-plans`
- 报价：`POST /checkout-quotes`
- 订单：`POST/GET /money-orders`、详情、取消、支付尝试与支付结果
- 权益：`GET /me/entitlements`、详情、`GET /me/usage-records`
- 固定核销：`POST /entitlement-resolutions`、消费意图创建/查询/开始
- 会员：当前会员、周期、升级预览、升级登记与历史
- 普通退款：`POST /refund-quotes`、`POST/GET /refunds`
- 运营：商业指标、对账 case、RBAC 修复命令和审计记录

接口边界使用公共枚举 `DAILY_ENERGY`、`COUNT`；领域内部仍使用 `DAILY_INSIGHT`、`DAILY_INSIGHT_CREDIT/READING_CREDIT`，转换只发生在 Controller。

## H5 页面

`/shop`、`/shop/detail`、`/checkout`、`/payment/result`、`/my/benefits`、`/my/orders`、`/my/membership`、`/my/refunds`、`/reading/prepare` 已完成真实 API 对接和静态构建。

## 已知限制

- 当前阶段按产品范围使用服务端确定性 Fake 跑通业务流程；真实微信公共能力已完成离线验证，但支付拉起、真实商户和资金对账后置到独立变更，未完成前只允许 `FAKE`。
- Fake 支付由服务端生成成功事实；H5 在 `provider=FAKE` 时不调用微信收银台，随后轮询订单的支付与交付状态。
- 会员升级按新订单全额支付；内部剩余价值只用于运营测算，不形成财务应付，也不进入用户页面。

## 配置收敛（2026-09-06）

- 已移除六个 `R11_*_ENABLED` 阶段开关及 API 路径拦截，商业功能随版本正常开放。
- 商品、报价、消费、订单、会员、退款接口继续受原有身份、参数和业务规则校验；支付回调、后台结算与补偿逻辑不变。
- 部署时删除环境文件中的六项历史变量；回滚需恢复上一版本及配套配置。验证证据见 `../testing/r1.1/2026-09-06-retire-rollout-flags.md`。

## 历史测试环境状态（2026-08-29）

- 后端与前端 `593eefe` 已部署至 `test-satori.shenxinyou.com`，功能提交已推送至 GitHub `main`。
- 当时六级灰度开关已按顺序开启（现已移除）；支付适配器保持 `FAKE`，测试环境配置为自动成功以验证完整交付闭环。
- opening grants 迁移与幂等重放通过，新旧 available/reserved 聚合一致，`SEED_BATCH_READ_MODE=LEGACY`。
- API/Worker/数据库兼容回滚演练通过；已恢复至新版本。
- 公网页面与 API Smoke 通过；到期 Outbox、未解决对账、权益负余额和种子负余额均为 0。

## 2026-09-08 旧种子扣费退役

- 代码：移除旧账扣费和模式切换；每日指引新建/重试统一消费，注册及赠礼只写新账，查询固定使用新账户。
- 部署准备：增加未结算旧任务、混合账本歧义检查与迁移后严格核验；更新停写、备份、迁移、验收和后续删表步骤。
- 服务器状态：待部署时执行数据迁移与验证；本次未修改服务器数据，旧表暂保留历史查询。
- 本地验证：构建、类型检查及改动文件 lint 通过；单元测试（排除原有模块边界失败文件）274 项、接口契约 28 项、前端渲染检查 69 项通过。全量单元测试原有 `legacy-miniapp-import.module.ts` 命名查找失败；全量 lint 原有消费测试、权益集成测试共 4 项错误。
- 未完成验证：本地 Docker 启动自动审批超时，未运行依赖 PostgreSQL/Redis 的数据库迁移、支付集成及端到端测试；部署前必须在隔离环境执行，不应将本地单元测试通过视为数据迁移验收通过。

## 2026-09-08 测试服务器隔离演练补充

- 测试服务器内完成数据库备份与隔离恢复；原测试服务在演练期间保持运行。
- 78 个账户两次迁移均无重复入账；严格预检、完整流水对账全部通过，迁移前后逐账户余额摘要、旧/新流水数量和批次数量一致。
- 修复两个实际演练发现：已双写注册赠礼的新账消费差异需凭源记录关联证明；历史流水时间乱序不能仅按最后快照判断余额。
- 隔离空库回归：种子、消费、支付集成测试 26 项通过，注册/每日指引/重试端到端测试 11 项通过。工作流测试独立模拟短信频控，真实频控代码未修改。
- 前端 lint（仅已有图片提示）、完整测试 135 项和静态构建通过；后端类型、构建、契约测试通过。
- 演练证据位于测试服务器 `/opt/satori-test/integration/seed-cutover-20260908/`；备份位于 `/opt/satori-test/backups/seed-cutover-20260908/`。实际切换另按手册执行停写后最终备份、迁移和健康检查。
