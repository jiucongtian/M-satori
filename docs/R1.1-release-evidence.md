# R1.1 发布证据与 Go/No-Go 清单

更新日期：2026-09-05

## 自动化结果

| 门禁 | 结果 |
| --- | --- |
| 后端全量测试 | 45 files / 284 tests，通过；8 files / 48 tests 按外部环境条件跳过 |
| H5 功能与契约测试 | 132 tests，通过 |
| H5 多端响应式 | 17 tests，通过；7 tests 按设备条件跳过；覆盖 8 种视口与 READ-13/15 的 1—5 张牌 |
| H5 埋点 E2E | 2 tests，通过；覆盖主导航上报、敏感字段保护与上报失败不阻断业务 |
| 运营平台结构测试 | 后端 34 tests + 前端 28 tests，通过 |
| 运营平台 E2E | 25 tests，通过；覆盖 desktop / tablet / mobile |
| 后端 typecheck/lint/build | 通过 |
| H5 lint/vinext build/static build | 通过，53 个静态输出（51 条业务路由 + 根页与 404） |
| 运营平台 build | 前后端构建通过 |

## 角色评审

| 角色 | 重点 | 当前状态 |
| --- | --- | --- |
| 后端 | 事务、账本、报告、对账、回滚兼容 | 类型、规范、测试与构建通过 |
| 前端 | 服务端定价、支付恢复、分享、问事与多端布局 | 功能、构建、埋点与响应式门禁通过 |
| 运营平台 | 账号权限、商品、订单、权益、数据分析 | 结构、构建与三端 E2E 通过 |
| 测试 | 主链路、异常恢复、1—5 张牌和埋点降级 | 本轮自动化回归通过 |
| 运维 | 拉取/合并、迁移、Nginx、回滚、监控 | 仍沿用 2026-08-29 测试环境部署证据；本轮代码待按发布流程部署 |
| 产品 | 商品、会员、核销、普通退款和升级口径 | 已确认当前阶段使用 Mock 跑通流程；业务体验仍可继续人工验收 |
| 客服 | 订单/支付/交付/权益/退款查询与处置 | 不阻塞 Mock 技术里程碑，运营演练可继续 |
| 财务 | 真实支付、渠道退款和资金对账 | 不属于当前 Mock 里程碑；真实微信接入时单独验收 |

## 当前结论

截至 2026-09-05，本轮代码质量结论为 **Regression Go**：已修复字体、构造参数、主导航埋点、运营平台退出文案、移动端操作热区和多牌阵小屏边界等门禁问题。真实手机微信支付已由业务方确认可用，本轮不重复开展正式环境登录与资金链路验收；该项仍需在正式发布前保留独立的支付、退款和财务对账签字。

## 历史测试环境执行证据（2026-08-29，仅用于追溯）

- 当时后端与前端不可变发布均为 `593eefe`，该提交仅代表 2026-08-29 的历史部署基线，不代表当前 `release/r1.1`。
- 上一后端 `3d1f043`、上一前端 `3abae6c` 和后端回滚镜像均保留，可原子回切。
- 部署前 `git pull --ff-only origin main` 已确认远程无新增提交和合并冲突；服务器按不可变归档发布。
- expand migration、法律文档 Seed、R1.1 商品 Seed 均成功；API、Worker、PostgreSQL、Redis 健康。
- opening grants：67 个账户，首次 50 migrated / 17 replayed / 0 blocked；第二次 0 migrated / 67 replayed / 0 blocked。
- 新旧种子汇总：账户数 67/67、available 1823/1823、reserved 0/0；读模型保持 `LEGACY` 可回退。
- 六个 Feature Flag 已按规定顺序逐项开启，每步从 503 进入 401 认证链路。
- 停单演练：新订单 503；订单读取 401、支付回调非 503、Worker 持续运行；恢复后新订单重新进入 401 认证链路。
- 兼容回滚：旧后端 `e15ebc4` 在 expand 后数据库上 ready=200、bootstrap=200；随后恢复 `f053136`，ready/bootstrap 均为 200。
- 最终公网：首页、ready、bootstrap、商城、下单、支付结果和权益页均为 200；Nginx 配置检查通过。
- 最终聚合：到期 Outbox 积压 0、未解决对账 case 0、权益负余额 0、种子负余额 0。
- 回调安全：用户认证不介入；仅放行精确 Docker 网关 `172.20.0.1`；缺少微信签名头由 Nginx 返回 400，带齐占位头后 `FAKE` 模式由 API 明确返回 503。真实商户仍保持关闭。
- 微信 API v3 离线门禁：商户请求 RSA 签名、微信应答验签、公钥 ID 匹配、AES-256-GCM 回调解密、主动查单、异步退款受理/查询均通过确定性测试；支付拉起形态和真实商户联调仍未完成。
- Mock 闭环：测试环境使用 `PAYMENT_PROVIDER_MODE=FAKE` 与 `FAKE_PAYMENT_RESULT=SUCCEEDED`；H5 识别 `FAKE` 后跳过 `WeixinJSBridge`，由服务端生成成功事实并触发 Outbox/Worker 交付。独立 PostgreSQL 测试已验证“下单 → 自动成功 → PAID → 权益发放 → FULFILLED → 恢复 opaque businessContext”。

Mock 测试里程碑已完成。后续真实支付变更需确认 JSAPI、H5 或双通道，完成真实商户下单/回调/查单/退款和财务对账，并通过面向真实资金的跨角色验收；在此之前测试与生产均不得启用 `PAYMENT_PROVIDER_MODE=WECHAT_PAY`。
