# R1.1 发布证据与 Go/No-Go 清单

更新日期：2026-08-29

## 自动化结果

| 门禁 | 结果 |
| --- | --- |
| 后端单元测试 | 29 files / 188 tests，通过 |
| OpenAPI 契约测试 | 1 file / 26 tests，通过 |
| PostgreSQL 集成测试 | 9 files / 39 tests，通过（独立干净数据库） |
| E2E | 3 files / 16 tests，通过（独立干净数据库） |
| H5 自动化测试 | 83 tests，通过（含 R1.1 商业与路由门禁） |
| 后端 typecheck/lint/build | 通过 |
| H5 lint/typecheck/vinext build/static build | 通过，25 个静态页面（最终静态构建使用 webpack 验证） |
| OpenSpec strict / OpenAPI lint | 通过 |
| R1.1 性能基线 | 全部门槛通过，见 `backend/R1.1-slo-and-release-gates.md` |

## 角色评审

| 角色 | 重点 | 当前状态 |
| --- | --- | --- |
| 后端 | 事务、账本、Saga、对账、回滚兼容 | 技术门禁与测试环境演练通过，待签字 |
| 前端 | 服务端定价、固定来源、支付恢复、用户文案 | 技术门禁与测试环境页面检查通过，待真机签字 |
| 测试 | 并发、乱序、崩溃恢复、跨日、最终补偿 | 自动化与测试环境回归通过，待签字 |
| 运维 | 拉取/合并、迁移、Nginx、回滚、监控 | 部署、灰度和回滚演练通过，待签字 |
| 产品 | 商品、会员、核销、普通退款和升级口径 | 待测试环境验收签字 |
| 客服 | 订单/支付/交付/权益/退款查询与处置 | 待运营后台演练签字 |
| 财务 | 支付事实、普通退款、异常补偿与对账 | 待真实微信测试商户及资金对账签字 |

## 当前结论

本地 Fake 支付链路、自动化门禁与测试环境技术演练为“条件 Go”。以下任一未完成均保持 Test No-Go：真实微信 API v3 适配器与测试商户联调未完成、远程发布提交未推送、跨角色签字未归档。

## 测试环境执行证据（2026-08-29）

- 后端不可变发布：`f053136`；前端不可变发布：`3abae6c`。
- 上一后端 `e15ebc4` 与上一前端 `a9e3c33-debug` 均保留，可原子回切。
- 部署前 `git pull --ff-only origin main` 已确认远程无新增提交和合并冲突；服务器按不可变归档发布。
- expand migration、法律文档 Seed、R1.1 商品 Seed 均成功；API、Worker、PostgreSQL、Redis 健康。
- opening grants：67 个账户，首次 50 migrated / 17 replayed / 0 blocked；第二次 0 migrated / 67 replayed / 0 blocked。
- 新旧种子汇总：账户数 67/67、available 1823/1823、reserved 0/0；读模型保持 `LEGACY` 可回退。
- 六个 Feature Flag 已按规定顺序逐项开启，每步从 503 进入 401 认证链路。
- 停单演练：新订单 503；订单读取 401、支付回调非 503、Worker 持续运行；恢复后新订单重新进入 401 认证链路。
- 兼容回滚：旧后端 `e15ebc4` 在 expand 后数据库上 ready=200、bootstrap=200；随后恢复 `f053136`，ready/bootstrap 均为 200。
- 最终公网：首页、bootstrap 与全部 R1.1 静态页面为 200；商城 API 未登录为 401；Nginx 配置检查通过。
- 最终聚合：到期 Outbox 积压 0、未解决对账 case 0、权益负余额 0、种子负余额 0。
- 回调安全：用户认证不介入；仅放行精确 Docker 网关 `172.20.0.1`；`FAKE` 模式回调明确返回 503。真实商户仍保持关闭。

测试环境部署与回滚演练已完成。当前 Test No-Go 仍包括：确认 JSAPI 或 H5 支付形态，完成真实微信 API v3 下单/查单/退款及加密回调适配并使用测试商户联调；完成财务/客服/产品/测试/运维签字；将本地提交推送到远程仓库。
