# R1.1 · P0 问事与交易契约基线

## 定位

本文件是后端接入前的**历史契约先行方案**。截至 2026-09-05，`release/r1.1` 已包含真实后端；当前契约为 `backend/openapi/openapi.yaml`，实现与测试边界见 [当前交接](../../R1.1-current-handoff.md)。下面的 JSON/Mock 仅保留追溯，不作为当前路由或业务覆盖率依据。

## 历史方案文件

- 接口契约：`backend/openapi/r1.1-p0-contract.json`
- Mock 场景：`backend/tests/contract/fixtures/r1.1-p0-scenarios.json`
- 可执行契约检查：`backend/tests/contract/r1.1-p0-contract.test.mjs`

## 已覆盖的 P0 规则

1. 问事创建、抽牌、生成和重试均拥有稳定任务标识与幂等键。
2. 抽牌后卡牌与位置冻结；重试不得重新抽牌或重复扣费。
3. 报告失败、离开页面、弱网恢复均保留原任务与卡牌。
4. 创建订单与支付回调均幂等；付款成功但权益未到账时必须可恢复，禁止重复购买。
5. 退款申请立即冻结未使用权益，订单、退款和流水状态一致。

## 原迁移要求与当前落实

- 当前问事契约已纳入 `backend/openapi/openapi.yaml`，不再使用旧 `/readings/{id}/generate` 方案。
- 问事关键场景已由 `card-reading-lifecycle.test.ts` 接入独立 PostgreSQL/Redis 验证；外部 Aqua 和真实支付仍需独立验收。
- 只有完成以上两项后，`R11-CORE-*`、`R11-PAY-*`、`R11-REFUND-*` 才可标记为“真实后端测试通过”。
