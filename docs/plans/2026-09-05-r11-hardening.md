# R1.1 业务闭环与架构收敛

基线：`release/r1.1@e3b4ba0`。用户授权按顺序修复审查问题；每块验证后提交、推送。部署另按发布 SOP 执行。

## 执行与验收

- [x] 1. 问事接入统一权益：抽牌前服务端预留，报告成功核销，失败释放；正式路由移除固定余额；重放返回原抽牌。异常中断恢复与并发生成在第 2 块完成。
- [x] 2. 问事接入持久化任务：复用任务表、Outbox、Worker；原子状态迁移、重启恢复和陈旧结果隔离；实现消费结果查询。
- [x] 3. 完善真实业务检查与反馈持久化：零权益、并发、失败/重试、恢复、反馈归属；修复过时断言，业务验证与源码检查分别记录。
- [x] 4. 收敛旧实现与文档：拆出正式问事组件，清理未装配重复逻辑，更新接口与交接状态。

## 修改前证据

- GitHub 与本地基线一致；工作区干净。
- GitNexus 已更新索引。CardReadingService、CardReadingController、ConsumptionModule、ReadingFlowScreen 影响为 LOW。
- ReadingPayment 为 HIGH：由 ReadingFlowScreen 与 LegacyProfileFlow 共享，间接关联档案页；保留接口并检查正式与旧页面。
- 基线后端 251 项单元测试和类型检查通过；前端专项 20/21，支付条件源码正则过时。

## 验证记录

第一块：后端类型检查、问事 12 项定向测试通过；前端类型检查、修改文件 ESLint 通过。新增迁移 0021。GitNexus 变更分析为 medium，仅问事主流程及相关数据结构。数据库业务验证将在任务接入后统一执行。

修复已完成。尚未宣称真实支付、外部 Aqua 或站点完整业务验收通过。

第二块服务器验证：`/opt/satori-test/integration/r11-hardening-20260905`，专用 PostgreSQL/Redis，未连接现有业务数据库。253 项单元测试、类型检查通过；问事生命周期与消费编排共 10 项数据库测试通过；修改文件 ESLint 通过。任务 targetId 使用当前消费意图 UUID，重试创建新消费意图并保留原牌面。报告先持久化为 SETTLING，再核销并变为 READY；自动重试与重启沿用上游幂等标识。

用户要求后续仅在测试服务器验证，已遵循。本机 Docker 已退出，未重新启动。测试服务器当前站点发布指针未更改。

第三块服务器验证：问事 6 项真实 PostgreSQL/Redis 生命周期测试通过（含实际 GenerationTaskWorker 执行、重复反馈与归属检查）；问事控制器与服务 13 项单元测试、现有登录/每日指引 12 项 HTTP 回归通过。前端接口校验与生成、类型检查、26 项问事/交易检查通过，其中源码断言仅证明接线，不作为端到端业务验收。接口类型改为由 OpenAPI 生成；消费者不能绕过权益调用运营直连解读接口。

第四块服务器验证：后端 264 项单元测试通过，保留的消费规则新增 10 项直接行为测试；问事生命周期增至 7 项（新增预留过期后沿用原牌继续），加消费编排 5 项，共 12 项数据库/队列测试通过。后端类型检查、修改文件 ESLint 和构建通过。前端 152 项页面/契约/状态检查通过，类型检查、vinext 构建与 53 页静态构建通过；图片相关既有 ESLint 提示不作为业务通过证据。

正式问事已从 LegacyProfileFlow 拆出，不再包含模拟成功定时器或占位报告；旧准备入口不再创建孤立消费，补购返回可恢复正式草稿。移除未装配编排、内存假 E2E 与两份无引用候选客户端，规则验证迁至真实使用的 helper，业务验证迁至 PostgreSQL/Redis。旧文件可从 Git 历史恢复；服务器测试目录旧副本保存在 retired/，未删除业务数据。

GitNexus 最终范围检查标记 Critical（共享页头与问事/商城兼容入口）；已核对依赖方向、运行关联回归和构建。首次增量索引失败后仅重建派生索引，未改动业务数据。当前交接统一至 `docs/R1.1-current-handoff.md`，旧文档明确标为历史。

## 提交

1. `f11e8bf`：统一权益预留与核销。
2. `31153fe`：持久化任务、陈旧结果隔离与结算恢复。
3. `532f400`：反馈保存、权限和真实队列验证。
4. 本文所在提交：正式/原型分离、删除未使用编排、旧入口及过期恢复、交接收敛。
