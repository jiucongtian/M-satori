# R1.1-REL-002：服务消费记录收敛测试部署

## 发布身份与范围

- 目标：测试环境 `test-satori.shenxinyou.com`。
- 候选：`release/r1.1@7507ef79c2622acb81301aa8a522f2ebc9415ae6`。
- 范围：用户端服务消费记录只展示最终扣费成功事件，并显示消费服务、扣减权益来源与次数、上海时区日期和时间。
- 内部权益账本仍保留预留、完成、释放、到期和调整等只追加流水；本次没有数据库结构变更，也不删除或改写历史数据。
- 结论：已发布；2026-09-05 19:08（Asia/Shanghai）完成切换和冒烟，产品体验待用户验收。

## 验证与部署证据

- GitNexus 变更审计风险为 medium；影响集中在权益消费记录查询、OpenAPI 契约和权益页展示，不改变余额投影或结算写流程。
- 测试只在服务器隔离目录执行：后端类型检查、264 项单元测试、6 项权益账本数据库集成测试通过；新增断言确认同一消费只向用户返回一条 `COMMIT` 记录。
- 前端 OpenAPI 校验与重新生成结果一致，TypeScript 和 Lint 通过；17 项商城页面检查、53 页测试版静态构建通过。Lint 只有 5 条既有图片优化提示，无错误。
- 服务器仓库在部署前执行 `fetch` 与 `pull --ff-only`，HEAD 核验为候选完整 SHA，未发现冲突或服务器热修复。
- 后端：`/opt/satori-test/current` → `/opt/satori-test/releases/7507ef7`。
- 前端：`/opt/satori-test/frontend/current` → `/opt/satori-test/frontend/releases/7507ef7`。
- API/Worker 镜像：`sha256:5bdaf87f743731ab1e6359ce38580e291f21f05fd5b13c30b542dcd2f9cf7455`，固定标签 `satori-backend:deployed-7507ef7`；两者运行且重启计数为 0。
- 迁移镜像：`sha256:063eb9ca937ab3599fed4c43d5c3ddb7969463aead735c46f0fb963cb1200dc2`，固定标签 `satori-backend-migrate:deployed-7507ef7`。
- 前端首页 SHA-256：`b238035beb1cd09b4ed152605e808fad91d9e95783a56ba19743e5cfef7c953d`。
- Nginx 配置校验成功；loopback ready、公共首页、Bootstrap 和权益页均为 HTTP 200，匿名消费记录接口按预期为 HTTP 401。

## 回滚与限制

- 上一版前后端 `ac711a7` 的发布目录与固定镜像继续保留；需要回滚时分别恢复前后端指针，并使用固定旧镜像启动。
- 本次无迁移变更，不需要数据库降级；仍禁止自动删除或改写账本历史。
- 自动化确认查询与展示约束，不代替用户登录后的真实移动设备视觉验收，也不代表真实微信支付、退款或外部 Aqua 工作流验收。
