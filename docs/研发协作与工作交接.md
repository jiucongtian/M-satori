# Satori 研发协作与工作交接

> 本文是 Satori 研发的长期协作与持续交接入口，不是一次性会议纪要。
>
> 面向角色：后端、前端、测试、产品
>
> 事实核验日期：2026-08-13
>
> 当前总体状态：**`release/r1.0` 已合并 `origin/main@5ac384d`，后端、真实 API 前端接入和测试环境基线已经形成；Aqua 每日指引适配器已在代码中实现，但测试环境仍默认使用确定性 Stub，尚无 Aqua Staging 端到端验收记录。R1.1 抽卡问事仍为前端与候选契约阶段。**

> 问题管理：**GitHub Issues 是唯一问题事实源。Codex 通过 GitHub 连接器读取和更新同一问题库；规则、标签、联调批次和测试汇总模板见 `docs/GitHub-Issues联调管理规范.md`。**

> 发布管理：代码或原型完成、单个或多个 Issue 准备合并验证时，统一执行 `docs/Satori-Release-SOP.md`。每次发布只建立一份 `docs/releases/<批次编号>.md` 总控记录，多个问题通过 GitHub `batch:*` 标签归入同一批次。

## 0. 重新开始工作时先做什么

无论距离上次研发过了多久，先按以下顺序恢复上下文：

1. 阅读本文的“当前快照”“立即下一步”和“待决策事项”。
2. 在仓库根目录执行下面的只读检查，确认文档没有过期：

   ```bash
   git status --short --branch
   git branch -vv --all
   git tag --list --sort=-creatordate
   git log --oneline --decorate --all -n 20
   find backend -maxdepth 3 -type f -print
   ```

3. 确认本次任务属于 R1.0 还是 R1.1，再选择工作分支；不要以当前端口号代替版本判断。
4. 检查未跟踪和未提交文件，不要在切分支时覆盖其他人或上一次留下的工作。
5. 开始实现前，将本次的目标、分支、负责人和验收方式写入文末“交接记录”。

## 1. 当前快照

### 1.1 仓库与工作树

| 项目 | 当前事实 | 交接含义 |
| --- | --- | --- |
| 正式代码仓库 | `jiucongtian/M-satori` | 本文、前端和后端代码的归属仓库 |
| 本地仓库路径 | `/Users/zhenghao/workspace/satori/satori` | 所有本文中的相对路径都以此为根 |
| 当前本地分支 | `release/r1.0` | 已包含代码合并点 `d815171`，其中合入 `origin/main@5ac384d`；本文提交位于该基线之后 |
| 当前本地状态 | 文档整理期间仅修改本文和 `docs/integrations/` | 状态具有时效性；任何切分支、合并或发布动作前必须重新执行 `git status` |
| 需求/原型仓库 | `jiucongtian/M-satoriSrs` | 本地同级目录 `../satorisrs`，`main@f757452` |
| 后端代码 | `backend/` | NestJS API/Worker、PostgreSQL/Redis、迁移、OpenAPI 3.1、契约/E2E 测试和 Docker Compose 已建立 |
| 真实联调 | 测试环境已完成核心 R1.0 主路径联调 | 前端通过同源 `/api/v1` 调用测试 API；详情见 `docs/backend/R1.0-test-deployment.md` |
| 内容生成 | Stub/Aqua 可配置，测试环境尚未完成 Aqua 验收 | `29ec485` 已实现 `AquaDailyInsightGenerator`；默认 `DAILY_INSIGHT_GENERATOR=STUB`，不得把代码接入等同于 Staging/生产可用 |

### 1.2 版本、分支与环境

| 项目 | R1.0 | R1.1 |
| --- | --- | --- |
| 产品定位 | 注册、建档、每日使用的基础闭环 | 在 R1.0 上新增“抽卡问事”完整前端闭环 |
| 维护分支 | `release/r1.0` | `release/r1.1` |
| 本地可见的远程跟踪分支头 | `origin/release/r1.0@d76c6c0` | `origin/release/r1.1@1d497d2` |
| 当前本地代码基线 | `release/r1.0` 已包含 `d815171` 和 Aqua 接入 `29ec485` | 未在本次核验中合并或变更 |
| 发布标签 | `frontend-r1.0.1` → `e92caf9`；历史标签 `frontend-r1.0.0` | 未打正式标签 |
| 前端本地端口 | `http://localhost:3001/` | `http://localhost:3011/` |
| 原型基准 | `http://localhost:3000/`，仅用于 H5 原型对照 | 同左 |
| 当前联调状态 | 核心路径已联调；仅测试域名 Go | 未联调 |

分支关系与规则：

- `release/r1.0` 是已冻结的 R1.0 前端基线，只接受 R1.0 缺陷修复。
- `release/r1.1` 从 `e2e7616` 演进，包含 R1.0 全部能力和 R1.1 抽卡问事。
- `origin/main@5ac384d` 已通过 `d815171` 合入当前 `release/r1.0`；当前分支同时保留 release 侧修复和 `29ec485` 的 Aqua 接入实现。
- R1.0 后端已在 `main` 建立并交付；后续 R1.0 修复应保持前后端契约、测试与交接文档同步，不得只修改其中一端。
- HTTP API 路径统一以 `/api/v1` 开头。页面上的 `R1.0` / `R1.1` 是产品追踪标签，不是 API 版本。
- 上表是 2026-08-13 执行 `git fetch origin --prune` 后的本地远程跟踪引用；后续仍需先保存工作区，再重新 fetch 核对。

### 1.3 代码和文档地图

| 路径 | 作用 | 当前状态 |
| --- | --- | --- |
| `frontend/` | 正式前端应用 | R1.0 在 `release/r1.0`；R1.1 在 `release/r1.1` |
| `frontend/src/api/client.ts` | 前端 API 客户端 | R1.0 核心路径已调用真实 `/api/v1`；契约类型来自生成文件 |
| `frontend/src/api/contracts/generated.ts` | 由 OpenAPI 生成的前端类型 | 当前 R1.0 接口契约基线 |
| `frontend/src/api/contracts/proposed.ts` | 历史 R1.0 候选契约 | 已由 OpenAPI 3.1 覆盖；新增字段不得以它为准 |
| `frontend/src/api/contracts/card-reading.ts` | R1.1 抽卡问事候选契约 | 仅存在于 `release/r1.1`，待评审 |
| `backend/` | 正式后端服务 | API/Worker、迁移、测试、Docker Compose 与运行说明已交付 |
| `backend/openapi/openapi.yaml` | R1.0 OpenAPI 3.1 | 当前可执行契约和前端类型生成源 |
| `backend/packages/modules/src/integrations/daily-insight/aqua-daily-insight.generator.ts` | Aqua 每日指引适配器 | 已实现 SDK 调用、输入转换、严格响应校验和错误映射；Staging 待验收 |
| `docs/integrations/Aqua-每日指引工作流需求说明.md` | Aqua 接入事实、契约与验收清单 | 已按 `29ec485` 当前实现更新；不得继续按旧的“待实现轮询适配器”描述执行 |
| `docs/api/H5与Satori后端接口文档.md` | API v1 讨论稿 | 历史设计参考；与 OpenAPI 冲突时以 OpenAPI 为准 |
| `docs/architecture/R1-系统架构方案.md` | R1 架构、模块、状态机和集成边界 | 后端设计基线 |
| `docs/R1.0-frontend-baseline.md` | R1.0 功能和上线门槛 | 已冻结 |
| `docs/backend/R1.0-interface-delivery-status.md` | R1.0 接口状态权威矩阵 | 已记录后端验证、前端集成、测试环境与待联调项 |
| `docs/R1.0-api-support-matrix.md` | R1.0 契约粗粒度矩阵 | 存在历史措辞；状态判断以接口状态矩阵和 OpenAPI 为准，后续应回填 |
| `openspec/changes/implement-r1-backend/` | R1.0 后端实施规格 | 已跟踪，含 proposal、design、tasks 和八份 capability spec |
| `../satorisrs/` | PRD、交互设计和 H5 原型 | 是产品/原型依据，不是正式代码目录 |

## 2. 状态口径

交接、Issue、OpenAPI 和联调表统一使用以下口径：

| 状态 | 含义 | 允许对外宣称“可用” |
| --- | --- | --- |
| `NOT_STARTED` | 无可运行实现 | 否 |
| `CONTRACT_DEFINED` | 讨论稿已定义，本轮未验证后端 | 否 |
| `CONTRACT_PROPOSED` | 前端候选契约，待后端评审 | 否 |
| `CONTRACT_FROZEN` | 双方已评审并纳入版本化 OpenAPI | 否 |
| `IMPLEMENTED` | 后端已实现且本地测试通过 | 否 |
| `DEPLOYED` | 已部署到可访问测试环境 | 仅可称“可联调” |
| `FRONTEND_ADAPTER_READY` | 前端有类型或调用适配器 | 否 |
| `MOCK_UI` | 页面由本地状态/演示数据驱动 | 否 |
| `INTEGRATED` | 前端已调用测试环境真实 API | 只表示已接入 |
| `VERIFIED` | 契约、集成、E2E 和关键异常路径通过 | 是 |

当前 R1.0 核心接口已达到 `CONTRACT_DEFINED/FROZEN + VERIFIED + INTEGRATED`（详见第 3 节）。这表示测试环境可联调，不等同于生产可发布：Aqua 适配器虽已实现，但测试环境启用、真实工作流响应、内容质量、故障补偿和监控告警仍需完成验收。

## 3. R1.0 产品边界和后端矩阵

R1.0 只包含以下九项：

1. 手机号登录与注册；
2. 创建本人生命智慧档案；
3. 四张关系卡牌结果；
4. 新用户智慧种子赠送；
5. 今日首页；
6. 每日指引生成与查看；
7. “我的”基本页面；
8. 生命智慧档案库基础能力；
9. 智慧种子账户及消费记录。

底部栏的“问事、成长、关系”在 R1.0 只是功能预告页，不是可用业务。

| 业务域 | 接口/接口组 | 契约 | 后端 | 前端 | 后端交付重点 | 阻塞 R1.0 |
| --- | --- | --- | --- | --- | --- | --- |
| 初始化 | `GET /app/bootstrap` | `CONTRACT_DEFINED` | `VERIFIED` | `INTEGRATED` | 配置、版本、可用功能 | 是 |
| 协议 | `GET /legal-documents/{documentId}` | `CONTRACT_DEFINED` | `VERIFIED` | `INTEGRATED` | 协议版本与正文 | 是 |
| 验证码 | `POST /auth/sms-challenges` | `CONTRACT_DEFINED` | `VERIFIED` | `INTEGRATED` | 限流、有效期、错误码、测试策略 | 是 |
| Session | `/auth/sessions*` | `CONTRACT_DEFINED` | `VERIFIED` | `INTEGRATED` | Access Token、Refresh Cookie、轮换与退出 | 是 |
| 当前用户 | `GET /me`、`PATCH /me/preferences`、`POST /me/consents` | `CONTRACT_DEFINED` | `VERIFIED` | `INTEGRATED` | 新老用户分流与协议状态 | 是 |
| 首页 | `GET /me/home-overview` | `CONTRACT_DEFINED` | `VERIFIED` | `INTEGRATED` | 种子摘要、今日指引状态 | 是 |
| 地点 | `GET /locations` | `CONTRACT_DEFINED` | `VERIFIED` | `INTEGRATED` | 标准城市、时区和搜索 | 是 |
| 本人档案 | `/me/life-profile*` | `CONTRACT_DEFINED` | `VERIFIED` | `INTEGRATED` | 预计算、确认、当前/历史版本 | 是 |
| 四张关系卡 | 随档案版本返回 | `CONTRACT_DEFINED` | `VERIFIED` | `INTEGRATED` | 稳定结构；年/月/日/时只是后端映射依据 | 是 |
| 注册赠种 | `/me/registration-reward*` | `CONTRACT_FROZEN` | `VERIFIED` | `INTEGRATED` | 确认自动/手动领取，幂等发放 | 是 |
| 档案库 | `/me/life-profiles*` | `CONTRACT_FROZEN` | `VERIFIED` | `INTEGRATED` | 多人档案、版本和所有权隔离 | 否 |
| 档案分组 | `/me/life-profile-groups*` | `CONTRACT_FROZEN` | `VERIFIED` | `MOCK_UI` | CRUD 和排序 | 否 |
| 种子账户 | `GET /me/wisdom-seed-account` | `CONTRACT_FROZEN` | `VERIFIED` | `INTEGRATED` | 可用、冻结、累计获得/消费 | 是 |
| 种子流水 | `GET /me/wisdom-seed-transactions` | `CONTRACT_FROZEN` | `VERIFIED` | `INTEGRATED` | 游标分页、业务来源、余额 | 是 |
| 每日指引 | `/daily-insights*` | `CONTRACT_FROZEN` | `VERIFIED（Stub）` + `IMPLEMENTED（Aqua 适配器）` | `INTEGRATED（Satori 任务轮询）` | Aqua SDK 调用已实现；Staging 真实工作流与内容验收待完成 | 是 |
| 反馈 | `POST /feedback` | `CONTRACT_DEFINED` | `VERIFIED` | `MOCK_UI` | 报告版本和防重 | 否 |
| 注销 | `/me/account-deletion-requests*` | `CONTRACT_DEFINED` | `VERIFIED` | `MOCK_UI` | 创建、查询和撤销申请 | 否 |

## 4. R1.1 产品边界和候选契约

R1.1 包含 R1.0 全部九项，并开放“抽卡问事”：问题与分类、1—5 张卡、系统随机抽取、按张数消耗智慧种子、翻牌、异步报告、失败恢复、历史、反馈和分享。

R1.1 **不包含**追问、提交追问和成长行动；也不提供用户选择具体卡牌或多套牌组。

| Method | Path | 用途 | 契约 | 后端 | 前端 | 关键要求 |
| --- | --- | --- | --- | --- | --- | --- |
| `POST` | `/card-draws` | 创建随机抽卡并冻结结果 | `CONTRACT_PROPOSED` | `NOT_STARTED` | `FRONTEND_ADAPTER_READY` + `MOCK_UI` | 服务端随机，客户端不可篡改，幂等 |
| `POST` | `/card-readings` | 根据抽卡结果创建报告 | `CONTRACT_PROPOSED` | `NOT_STARTED` | `FRONTEND_ADAPTER_READY` + `MOCK_UI` | 校验归属，预占种子，返回异步任务 |
| `GET` | `/card-readings/{readingId}` | 读取状态或报告 | `CONTRACT_PROPOSED` | `NOT_STARTED` | `FRONTEND_ADAPTER_READY` + `MOCK_UI` | 仅本人可访问，稳定五段式结构 |
| `GET` | `/card-readings?cursor=...` | 问事历史 | `CONTRACT_PROPOSED` | `NOT_STARTED` | `FRONTEND_ADAPTER_READY` + `MOCK_UI` | 游标分页，创建时间倒序 |
| `POST` | `/card-readings/{readingId}/retry` | 重试失败任务 | `CONTRACT_PROPOSED` | `NOT_STARTED` | `FRONTEND_ADAPTER_READY` + `MOCK_UI` | 不重复扣费，明确可重试状态 |
| `POST` | `/card-readings/{readingId}/feedback` | 提交反馈 | `CONTRACT_PROPOSED` | `NOT_STARTED` | `FRONTEND_ADAPTER_READY` + `MOCK_UI` | 幂等/防重，绑定报告版本 |

候选核心字段：

- 抽卡：`question`、`category`、`cardCount(1..5)`、`deckCode=RELATIONSHIP_WISDOM_DEFAULT`、`drawMethod=SYSTEM_RANDOM`、`positionLabels`；
- 生成：`drawId`、`audience=CONSUMER`、`settlement.currency=WISDOM_SEED`、`settlement.amount`；
- 反馈：`CLEARER | INSPIRED | NEEDS_TIME | NOT_HELPFUL`；
- 报告五段：`OPENING | SELF | CARDS | ACTION | CLOSING`。

需在契约评审时确认抽卡报告是否复用 `/generation-tasks/{taskId}` 和 SSE。如复用，必须共用任务状态、错误码、心跳、断线重连和重试规则，不建第二套任务系统。

## 5. 不可违背的跨端约束

### 5.1 智慧种子结算

每日指引和抽卡问事必须共用一套账务模型：

1. 接受任务前校验可用余额；
2. 接受后预占，成功可交付后核销；
3. 失败、取消或不可交付时释放/退回；
4. 同一 `Idempotency-Key` 重放不得重复扣费；
5. 账户余额与流水在同一事务边界内写入；
6. 流水保存业务类型、资源 ID、幂等键和变更后余额；
7. 最终余额以后端响应为准，前端不自行推算。

结算状态统一为 `RESERVED | CONSUMED | RELEASED | REFUNDED`；流水类型为 `GRANT | RESERVE | CONSUME | RELEASE | REFUND | ADJUSTMENT`。所有内容生成只用智慧种子结算，不直接现金支付。

### 5.2 安全、数据和任务

- 所有私有资源都要在服务端校验当前用户和资源归属。
- 生成输入、模型/提示版本、生成结果和结算记录必须可追溯。
- 抽卡结果由服务端产生并冻结，不接受 H5 指定具体牌。
- 异步任务必须有明确终态，查询和 SSE 返回的状态语义必须一致。
- Token 过期、限流、幂等重放、余额不足、生成失败和不可重试都要有稳定错误码。

## 6. 实施顺序与立即下一步

### 6.1 当前工作队列

| 顺序 | 任务 | 负责角色 | 完成标志 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 确定后端技术基线、开发分支和环境约定 | 后端 | ADR/设计入库，分支可追踪 | `DONE` |
| 2 | 完成 OpenSpec 的 specs / design / tasks，并评审 R1.0 候选契约 | 后端 + 前端 | 决策有记录，候选契约无未决字段 | `DONE` |
| 3 | 建立后端可运行骨架 | 后端 | 健康检查、配置、迁移、统一错误、日志和 CI 可运行 | `DONE` |
| 4 | 冻结并发布 R1.0 OpenAPI 3.1 | 后端 + 前端 | 可导入，契约测试和 breaking-change 检查可运行 | `DONE` |
| 5 | 打通登录、`/me`、地点和本人建档 | 后端 | 首次/回访用户的 API 集成测试通过 | `DONE` |
| 6 | 完成注册赠种、账户流水和每日指引结算 | 后端 | 幂等、成功、失败释放和并发测试通过 | `DONE（Stub 已验证，Aqua 路径已实现）` |
| 7 | 完成档案库与分组 | 后端 | 多人档案 CRUD、版本、排序和权限隔离通过 | `DONE` |
| 8 | 部署 R1.0 测试环境并前端去 Mock | 后端 + 前端 | Base URL/测试账号可用，前端主路径接真实 API | `DONE（核心路径）` |
| 9 | R1.0 联调验收 | 后端 + 前端 + 测试 | 本文 8.1 全部通过 | `IN_PROGRESS` |
| 10 | 对接 Aqua 每日指引 Workflow 并完成内容验收 | 后端 + AI/内容 | Staging SDK 调用、Schema、安全内容、账务补偿和版本追踪验收通过 | `IN_PROGRESS（代码完成，环境验收待办）` |
| 11 | 生产短信、Aqua、监控告警与发布评审 | 后端 + 运维 + 产品 | 生产环境 Go/No-Go 通过 | `BLOCKED_BY_10` |
| 12 | 冻结 R1.1 抽卡契约并复用通用任务/结算 | 后端 + 前端 | R1.1 OpenAPI 及契约测试通过 | `BLOCKED_BY_R1.0_FOUNDATION` |

### 6.2 下一个可执行动作

**下一步优先在测试环境安全注入 Aqua service key，显式设置 `DAILY_INSIGHT_GENERATOR=AQUA`，完成成功、超时/限流、非法响应和账务释放的 Staging 联调；验收完成前继续以 Stub 作为测试环境默认值。**

并行补齐档案分组、反馈和注销页面的真实 API 接入；它们当前后端已验证，但前端仍为 `MOCK_UI`，不阻塞 R1.0 主路径。

## 7. 前后端交付物

### 7.1 后端每个联调批次交付

- OpenAPI 3.1 文档和与之对应的 Git commit；
- 测试环境 API Base URL、部署版本和数据库迁移版本；
- CORS、Cookie 域、HTTPS、短信测试码和测试账号配置；
- 本批次接口的精确状态、错误码、枚举、幂等、并发和限流规则；
- SSE 事件格式、心跳、断线重连、终态和测试触发方法；
- 可校验的智慧种子账户/流水以及生成成功、失败、超时、重试场景；
- 测试数据初始化/清理方法，以及回滚方案。

### 7.2 前端每个联调批次交付

- 本批次覆盖的页面、路由和前端 commit；
- 对 OpenAPI 的字段/枚举/错误码评审结果；
- 真实 API 适配器、Mock 移除列表和仍保留 Mock 的原因；
- 主路径、空态、错误态、重试和 Token 过期的验证记录；
- 前端构建/测试结果以及未关闭问题。

### 7.3 每次交接的最小信息

“接口做完了”不能作为交接。每个批次至少要同步：

| 字段 | 必填内容 |
| --- | --- |
| 范围 | 版本、业务域、接口列表 |
| 代码 | 仓库、分支、commit/tag |
| 契约 | OpenAPI 版本/文件和变更概要 |
| 环境 | Base URL、部署版本、迁移版本 |
| 验证 | 已执行的测试、结果和证据链接 |
| 数据 | 测试账号、初始化/清理方法（不在文档中写真实密钥） |
| 限制 | 已知问题、未覆盖路径、风险 |
| 下一步 | 具体负责人、动作和预期产物 |

## 8. 联调与验收

### 8.1 R1.0

- [x] 新手机号可以获取验证码、注册并建立 Session（测试环境固定测试验证码）。
- [x] 老用户登录后直接进入首页。
- [x] 首次用户完成档案预计算、确认并得到四张关系卡。
- [x] 注册赠种只入账一次，重试和并发请求不重复入账。
- [x] 每日指引在 Stub 成功时准确扣种，Stub 失败/超时时余额恢复；Aqua 适配器单元测试通过。
- [x] 本人及他人生命智慧档案可按权限查询和维护。
- [x] 首页摘要、种子账户与流水最终一致。
- [x] Token 过期、重复提交、余额不足和生成失败有稳定错误码。
- [ ] Aqua Workflow 已在 Staging 完成契约、内容和安全验收；生产开关可开启。
- [ ] 档案分组、反馈和注销页面完成真实 API 接入（非 R1.0 主路径阻塞项）。

### 8.2 R1.1

- [ ] 1—5 张牌都由服务端随机产生并冻结。
- [ ] 客户端修改请求无法指定抽卡结果。
- [ ] 抽卡、报告和当前用户正确绑定，越权访问被拒绝。
- [ ] 按卡数扣除种子，幂等重放和失败重试不重复扣费。
- [ ] 任务轮询/SSE、断线恢复、失败重试和最终交付可验证。
- [ ] 历史、详情、反馈和分享数据可正确读取。
- [ ] 报告不包含 R1.1 排除的追问和成长行动。

## 9. 当前阻塞和待决策事项

### 9.1 阻塞项

1. Aqua SDK 适配器已经实现，但测试环境尚无启用 `DAILY_INSIGHT_GENERATOR=AQUA` 后的端到端证据；真实响应 Schema、内容质量、超时/限流、账务补偿和版本追踪仍待 Staging 验收。
2. 生产短信、Aqua、监控告警和生产 Go/No-Go 尚未完成；测试域名的验证不等同于生产发布批准。
3. 档案分组、反馈和注销页面仍为 `MOCK_UI`，虽然对应后端接口已验证。
4. 本人档案 preview/confirm 尚不接收用户填写的称呼，服务端固定返回“我的生命智慧档案”。

### 9.2 需要明确记录的决策

| 决策 | 当前状态 | 建议产物 |
| --- | --- | --- |
| 后端技术栈、数据库、部署平台 | 已实现于 `backend/` 与测试环境 | 后续生产环境变更需更新 Runbook |
| 后端开发分支与合并策略 | `origin/main@5ac384d` 已合入当前 `release/r1.0` | 后续合并继续同步契约、测试与本文 |
| 注册赠种发放方式 | `CONTRACT_FROZEN` | 以 OpenAPI 3.1 和幂等规则为准 |
| 档案库、分组、种子契约 | `CONTRACT_FROZEN` | 以 OpenAPI 3.1 和契约测试为准 |
| 首页种子/今日指引字段 | 已定义并集成 | 以 `home-overview` schema 为准 |
| 每日指引价格和扣费时机 | 已冻结为 1 颗智慧种子，Stub 账务已验证 | Aqua Staging 启用后复验生成失败/补偿 |
| R1.1 是否复用通用生成任务和 SSE | 待后端评审 | ADR + OpenAPI |
| 前端认证是否保留当前 Sites/ChatGPT 相关骨架 | 待架构确认 | 身份边界 ADR，避免与手机号 Session 冲突 |

## 10. 本地运行与验证备忘

前端要求 Node.js `>=22.13.0`。在对应 Release 分支上执行：

```bash
cd frontend
npm ci
npm run dev
npm run lint
npm test
```

- R1.0 的 `npm run dev` 默认端口是 `3001`。
- R1.1 的 `npm run dev` 默认端口是 `3011`。
- 后端的本地运行、迁移、测试和测试环境部署命令见 `backend/README.md`、`docs/backend/R1.0-test-deployment.md` 与 `docs/backend/R1.0-operations-runbook.md`。不要在本文复制环境密钥或测试账号。
- 端口只是本地前端约定；API 版本永远以 `/api/v1` 路径和 OpenAPI 版本为准。

## 11. 交接记录

更新规则：

1. 每次完成一个可联调批次、改变分支/环境、冻结契约或发现新阻塞时，同一个 PR 内更新本文。
2. 先更新“当前快照”和工作队列，再在本节倒序增加记录。
3. 状态只能根据可复现证据升级；至少记录 commit、环境/版本和测试结果。
4. 凭据、密钥、真实手机号和 Token 不得写入本文或 Git。

### 2026-08-14 — 四柱计算内核升级为 localCalculateBazi v1.3

- 范围：R1.0 本人生命智慧档案，H5 出生资料预览所调用的后端四柱计算。
- 代码：`release/r1.0@6cd2ca5`，已提交并推送至 `origin/release/r1.0`。
- 已完成：将 `localCalculateBazi_v1_3` 的年柱、月柱、日柱和时柱规则移植为后端独立模块；年/月柱采用分钟级节气时刻和“节气落在出生时辰内则整个时辰按节后”的原规则；保留公历/农历输入和不确定时间能力。地点/时区仍写入档案快照，但四柱严格按输入民用时间计算，不做真太阳时校正，因此相同出生日期时间在不同城市返回相同八字。
- 算法版本：新生成快照记录 `localCalculateBazi/v1.3+civil-time/r1.0`，历史快照保持不可变。
- 已验证：新旧黄金案例、立春所在时辰边界和完整前置时辰用例通过；与源云函数跨 1900—2100 年抽样 64 组结果一致；后端 typecheck、lint、build 通过，全量测试 61 项通过、10 项环境相关跳过。
- 环境：已部署至测试环境 `https://test-satori.shenxinyou.com`；后端发布目录为 `/opt/satori-test/releases/6cd2ca5`，`/opt/satori-test/current` 已原子切换至该版本。
- 已验证：服务器本机 `/api/v1/health/ready` 返回 `{"data":{"status":"ok"}}`；API 容器 healthy、Worker 运行；公网首页与 `/api/v1/app/bootstrap` 均返回 HTTP 200。
- 下一步：使用业务确认案例完成 H5 建档与四柱结果端到端复核。

### 2026-08-13 — 合并 main 并校准 Aqua 接入状态

- 范围：R1.0 分支同步、登录分流冲突处理、Aqua 每日指引代码与文档状态。
- 代码：`release/r1.0` 通过 `d815171` 合入 `origin/main@5ac384d`；Aqua 适配器实现来自本地提交 `29ec485`。
- 已完成：保留 release 侧完整 `nextAction` 与注册奖励逻辑，接入 main 的主体/修订结构；前端以 `/me.nextAction` 为主并兼容 Session 回退；确认 `AquaDailyInsightGenerator`、SDK 0.1.1、环境开关和严格响应校验已经实现。
- 已验证：后端 typecheck 通过，58 项测试通过、10 项环境相关跳过；前端 lint、构建和 21 项渲染测试通过。
- 环境事实：测试环境此前完成 Stub 主路径验证；本次没有启用 Aqua service key，也没有真实 Aqua Staging 请求证据。
- 文档：本文与 `docs/integrations/Aqua-每日指引工作流需求说明.md` 已区分“适配器已实现”和“Staging/生产未验收”。
- 下一步：安全配置 Aqua Staging，完成成功、重试、非法响应、安全拒绝、种子补偿和监控验收，再决定生产 Go/No-Go。

### 2026-08-11 — HOME-01 最新原型同步

- 范围：R1.0 / R1.1，页面 `HOME-01`。
- 代码：R1.0 `main` 与 `release/r1.0` 均更新至 `e92caf9`；R1.1 `release/r1.1` 更新至 `1d497d2`。
- 已完成：同步最新高/中/低能量指引卡、今日建议、适合事项、注意事项和入口按钮；R1.0 保留真实种子余额、每日指引状态和真实 API 入口；R1.1 保留既有抽卡问事范围。
- 已验证：R1.0 lint、Vinext 构建、12/12 自动化测试与 Next 静态导出通过；冻结版 `release/r1.0` 10/10；R1.1 13/13。新增 HOME-01 原型样式和内容回归用例。
- 环境：原型 `http://localhost:3000/`；R1.0 测试环境 `https://test-satori.shenxinyou.com`。
- 已部署：通过 SSH 别名 `zh-tencent-openClaw` 将 `e92caf9` 的静态产物发布至不可变目录 `/opt/satori-test/frontend/releases/e92caf9-home01-20260811`，并原子切换 `/opt/satori-test/frontend/current`。
- 已验证：`nginx -t` 与 reload 成功；公网首页和 `/api/v1/app/bootstrap` 均返回 HTTP 200；部署资源中已核验 HOME-01 新标题和入口文案。自动化应用内浏览器会话超时，HTTP、接口及静态资源验证不受影响。
- 回滚点：`/opt/satori-test/frontend/releases/044d178-home-login-20260811`。本次未触发回滚。
- 发布标签：`frontend-r1.0.1`，指向本次部署源提交 `e92caf9`。
- 下一步：进入 R1.0 与后端真实接口的回归联调；如 HOME-01 再修改，继续按不可变目录、原子软链接、Nginx 检查和公网双端点验证流程发布。

### 2026-08-11 — 回填 R1.0 后端、测试环境与前端集成状态

- 范围：R1.0 API、Worker、测试环境和核心前端路径。
- 代码：`main@044d178` 已包含后端交付；当前本地 `release/r1.0@a4b7659` 含一个未推送的首页回归修复。
- 契约/数据库：`backend/openapi/openapi.yaml` 为 OpenAPI 3.1 基线；数据库迁移、契约测试和核心 E2E 已交付。
- 环境：测试环境 API/Worker/PostgreSQL/Redis 与 HTTPS 健康；Base URL、测试步骤与回滚方式见 `docs/backend/R1.0-test-deployment.md`。
- 已完成：认证、建档、四张关系卡、赠种、种子账务、每日指引任务/轮询/查询、首页和档案库已接真实测试 API；档案分组、反馈和注销后端已验证。
- 已验证：接口状态见 `docs/backend/R1.0-interface-delivery-status.md`；核心主路径完成浏览器联调，契约、E2E 和异常路径有自动化证据。
- 已知问题：每日指引当前由确定性 Stub 生成，尚未接入 Aqua；档案分组、反馈、注销页面仍为 `MOCK_UI`；本人档案称呼暂为服务端固定值。
- 下一步：完成 Aqua Staging Workflow 契约、内容与安全验收，并补齐非阻塞页面真实 API 接入。

### 2026-08-10 — 建立可恢复的交接基线（历史快照）

- 仓库/分支：`M-satori` / `main@8689375`。
- 已核验：R1.0 分支头 `e2e7616`；R1.1 分支头 `560da2a`，前端功能基线 `9254616`。
- 前端：R1.0 界面基线已冻结；R1.1 抽卡问事界面与 TypeScript 候选契约已形成；页面仍为 Mock 驱动。
- 当时后端：`backend/` 只有 `.gitkeep`，无可运行实现、测试环境或 OpenAPI。
- 当时本地草稿：`openspec/changes/implement-r1-backend/proposal.md` 已形成，但 `openspec/` 仍未跟踪，不能当作已批准方案。
- 后续结果已由 2026-08-11 记录覆盖；不得把本条历史快照当作当前状态。

### 后续记录模板

```md
### YYYY-MM-DD — <交付批次/里程碑>

- 范围：<R1.0/R1.1 + 业务域>
- 代码：<分支 + commit/tag>
- 契约/数据库：<OpenAPI 版本 + migration 版本>
- 环境：<Base URL + 部署版本>
- 已完成：<可验证的结果>
- 已验证：<命令/用例 + 结果>
- 已知问题：<问题、影响和临时方案>
- 下一步：<负责角色 + 具体动作 + 完成标志>
```

## 12. 参考优先级

信息冲突时按以下顺序处理：

1. 已批准的 PRD/产品决策和版本化 OpenAPI；
2. 已合并的代码、数据库迁移和自动化测试；
3. `docs/architecture/R1-系统架构方案.md`；
4. `docs/api/H5与Satori后端接口文档.md`（当前是讨论稿）；
5. `frontend/src/api/contracts/` 中的候选类型；
6. 本文的进度快照与交接记录；状态详情以 `docs/backend/R1.0-interface-delivery-status.md` 为准。

关联文件：

- `docs/R1.0-frontend-baseline.md`
- `docs/R1.0-api-support-matrix.md`
- `docs/backend/R1.0-interface-delivery-status.md`
- `docs/backend/R1.0-test-deployment.md`
- `docs/backend/R1.0-slo-and-release-gates.md`
- `docs/integrations/Aqua-每日指引工作流需求说明.md`
- `docs/R1.0-visual-parity.md`
- `docs/architecture/R1-系统架构方案.md`
- `docs/api/H5与Satori后端接口文档.md`
- `backend/openapi/openapi.yaml`
- `openspec/changes/implement-r1-backend/`
