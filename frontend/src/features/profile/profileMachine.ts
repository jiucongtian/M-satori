export type ProfileState = "intro" | "editing" | "calculating" | "confirming" | "first-look" | "gift" | "complete" | "failed";
export type ProfileEvent = { type: "START" | "EDIT" | "PREVIEW" | "PREVIEW_READY" | "CONFIRMED" | "FIRST_LOOK_DONE" | "COMPLETE" | "RETRY" } | { type: "FAIL"; recoverTo: "editing" | "calculating" | "confirming" };
export type ProfileMachine = { state: ProfileState; recoverTo: "editing" | "calculating" | "confirming" };
export const initialProfileMachine: ProfileMachine = { state: "intro", recoverTo: "editing" };
const allowed: Record<ProfileState, Partial<Record<ProfileEvent["type"], ProfileState>>> = { intro:{START:"editing"},editing:{PREVIEW:"calculating"},calculating:{PREVIEW_READY:"confirming",FAIL:"failed"},confirming:{CONFIRMED:"first-look",EDIT:"editing",FAIL:"failed"},"first-look":{FIRST_LOOK_DONE:"gift",EDIT:"editing"},gift:{COMPLETE:"complete"},complete:{},failed:{RETRY:"editing",EDIT:"editing"} };
export function profileReducer(machine:ProfileMachine,event:ProfileEvent):ProfileMachine{if(event.type==="FAIL")return{state:"failed",recoverTo:event.recoverTo};if(event.type==="RETRY"&&machine.state==="failed")return{...machine,state:machine.recoverTo};const next=allowed[machine.state][event.type];return next?{...machine,state:next}:machine;}
