"use client";import { useEffect,useState } from "react";import { useRouter } from "next/navigation";import { api,type DailyInsight,type HomeOverview,type WisdomSeedAccount } from "@/src/api/client";import { DailyReport } from "@/src/features/legacy/LegacyProfileFlow";import { ProtectedRoute } from "@/src/shared/guards";import { dailyReportReturnPath,isIsoDate,ROUTES,type AppPath } from "@/src/shared/routes";import { RouteError,RouteFrame,RouteSkeleton } from "@/src/shared/shell";import { apiMessage } from "@/src/shared/ui";
export default function DailyReportScreen(){const[date,setDate]=useState<string|null>(null);const[returnPath,setReturnPath]=useState<AppPath>(ROUTES.home);const[ready,setReady]=useState(false);const[insight,setInsight]=useState<DailyInsight|null>(null);const[home,setHome]=useState<HomeOverview|null>(null);const[account,setAccount]=useState<WisdomSeedAccount|null>(null);const[error,setError]=useState("");const router=useRouter();useEffect(()=>{const timer=window.setTimeout(()=>{const query=new URLSearchParams(window.location.search);setDate(query.get("date"));setReturnPath(dailyReportReturnPath(query.get("from")));setReady(true);},0);return()=>window.clearTimeout(timer);},[]);useEffect(()=>{if(!isIsoDate(date))return;let active=true;void Promise.all([api.dailyInsight(date),api.home(),api.seedAccount()]).then(([daily,overview,seeds])=>{if(active){setInsight(daily);setHome(overview);setAccount(seeds);}}).catch(reason=>{if(active)setError(apiMessage(reason));});return()=>{active=false;};},[date]);if(!ready)return <RouteSkeleton/>;if(!isIsoDate(date))return <RouteError title="报告地址无效" message="没有找到可恢复的每日指引。" backHref={returnPath}/>;if(error)return <RouteError title="报告暂时无法打开" message={error} backHref={returnPath}/>;if(!insight||!home)return <RouteSkeleton label="正在恢复每日报告…"/>;return <ProtectedRoute><RouteFrame title="每日报告" label="每日报告"><div className="profile-flow"><DailyReport name={home.profile.displayName||"你"} insight={insight} energyLevel={home.dailyEnergySummary.data?.energyLevel} heavenCard={home.dailyEnergySummary.data?.heavenCard} balance={account?.available??null} onBack={()=>router.replace(returnPath)} onNext={()=>router.replace(ROUTES.home)} onShare={()=>router.push(`/share/generating?type=daily&date=${encodeURIComponent(date)}`)}/></div></RouteFrame></ProtectedRoute>;}
