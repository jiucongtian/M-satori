import ReadingNewScreen from "@/src/features/reading/ReadingNewScreen";

export const metadata = { title: "开始问事 · 初见 FRESH" };

export default function ReadingNewPage() {
  return <ReadingNewScreen />;
}
