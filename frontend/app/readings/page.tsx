import ReadingHomeScreen from "@/src/features/reading/ReadingHomeScreen";

export const metadata = { title: "问事 · 初见 FRESH" };

export default function ReadingsPage() {
  return <ReadingHomeScreen />;
}
