## 1. 建立基线与迁移护栏

- [x] 1.1 盘点现有 H5 的页面状态、入口、跳转、接口调用和写操作，形成旧状态到目标路由/域内状态的迁移矩阵，并标记必须保持不变的业务行为。
- [x] 1.2 为登录、隐私同意、首页、建档、每日任务和“我的”核心流程补充迁移前回归用例，覆盖首次进入、刷新、浏览器前进后退、重复点击和接口失败重试。
- [x] 1.3 记录当前生产构建的首页与各主要流程 JS/CSS 体积、请求数和关键加载指标，作为后续性能预算的比较基线。
- [x] 1.4 增加构建产物分析与性能预算检查：首页 gzip 后 JS+CSS 不高于当前约 263 KB 且目标至少降低 25%，任一业务域新增 gzip JS 目标不超过 100 KB；预算超限时使 CI 失败。

## 2. 建立共享路由基础设施

- [x] 2.1 定义集中式路由常量、公共/受保护路由清单、允许的 `next` 目标和查询参数校验器，拒绝外部地址、未知路由及敏感字段。
- [x] 2.2 实现统一 `SessionProvider`，明确 `unknown`、`anonymous`、`authenticated`、`consent-required` 状态，并确保初始化期间显示稳定骨架而非错误页面闪烁。
- [x] 2.3 实现可复用的认证与隐私同意守卫，统一登录后返回、同意后返回、退出登录清理和 `replace` 跳转规则。
- [x] 2.4 实现共享页面壳、加载态、错误态、空态、页面标题、路由切换焦点管理和不含敏感数据的路由诊断日志。
- [x] 2.5 为安全返回地址、开放重定向拦截、公共页面非阻塞加载、受保护页面守卫和退出清理补充单元/集成测试。

## 3. 按业务域拆分代码但暂不改变行为

- [x] 3.1 建立 `src/features/auth`、`home`、`profile`、`daily`、`my` 和共享模块目录，将模块边界与设计文档中的路由所有权保持一致。
- [x] 3.2 从现有单体页面提取品牌外壳、导航、错误处理、接口访问及通用展示组件，并用适配层保持原有调用方式可运行。
- [ ] 3.3 将全局样式拆分为基础 token、公共壳样式和按业务域加载的样式，验证视觉表现及移动端断点没有回退。
- [x] 3.4 为提取后的模块运行现有回归测试和构建体积对比，确认该阶段不改变路由、接口参数、写操作时机及用户可见行为。
- [x] 3.5 将本阶段作为独立可回滚提交推送到远程仓库，并记录基线测试结果。

## 4. 迁移公共页与认证域路由

- [x] 4.1 创建静态导出兼容的 `/`、`/login`、`/consent` 和 `/legal` 页面入口，使每个入口只加载本页必需模块。
- [x] 4.2 实现登录、隐私同意、法律文档和安全 `next` 返回流程，区分用户主动导航的 `push` 与守卫/完成态跳转的 `replace`。
- [x] 4.3 验证四个固定路由可直接访问、刷新、前进后退和从未登录状态恢复，并验证非法 `documentId`/`next` 有安全降级。
- [x] 4.4 删除已迁移公共页和认证页对应的旧 `view` 分支、重复初始化和不可达导入，同时保留尚未迁移业务域的兼容入口。
- [x] 4.5 将本阶段作为独立可回滚提交推送到远程仓库，并记录路由及回归测试结果。

## 5. 迁移首页与“我的”只读业务域

- [x] 5.1 创建 `/home`、`/my`、`/my/profile`、`/my/seeds`、`/my/reports` 和 `/my/archive` 固定路由，并通过认证/同意守卫保护需要授权的页面。
- [x] 5.2 将首页和“我的”数据读取提取为域级查询逻辑，统一加载、空数据、失败重试、缓存失效和并发去重行为。
- [x] 5.3 验证域内导航、跨域导航、直接访问、刷新、前进后退以及无权限/数据缺失场景，确保只读页面不会触发创建、领取或扣减类写操作。
- [ ] 5.4 移除已迁移首页和“我的”页面的旧状态分支及重复资源，确认入口构建不再静态导入这些受保护业务域。
- [x] 5.5 运行构建与性能预算检查，在部署前拉取并核对测试服务器最新版本，再以独立可回滚提交推送并部署该阶段到测试环境验证。

## 6. 将建档流程改为域内状态机

- [x] 6.1 用具名状态、事件和守卫定义建档 reducer，替换数字步骤索引，并为非法事件和缺失上下文提供可预测的恢复状态。
- [x] 6.2 为未提交草稿实现带版本号、过期时间和用户边界的 `sessionStorage` 持久化；敏感字段不得进入 URL、`localStorage` 或日志，并在完成、退出或身份变化时清理。
- [x] 6.3 创建 `/profile/create` 路由，将表单、预览、确认、首次查看和赠送结果映射为域内状态，而不是继续拆成公开 URL。
- [x] 6.4 根据后端已存在的档案、预览/确认结果和权益状态恢复流程，防止刷新、组件重挂载、Strict Mode 或双击造成重复预览、确认、领取或创建。
- [x] 6.5 补充状态转换、草稿过期/损坏、接口错误重试、离开再进入、刷新及浏览器后退测试，并核对现有接口契约和业务结果不变。
- [ ] 6.6 删除建档域对应的旧 `profileSteps`、数字 `setStep` 分支和兼容代码，将该阶段作为独立可回滚提交推送到远程仓库。

## 7. 将每日任务改为域内状态机

- [x] 7.1 用具名状态、事件和守卫定义每日任务 reducer，覆盖任务加载、选择、确认、生成中、报告完成、失败重试和不可用状态。
- [x] 7.2 创建 `/daily` 和 `/daily/report?date=YYYY-MM-DD` 路由，对日期和资源标识进行白名单校验，对无效或无权限报告安全降级。
- [x] 7.3 依据后端任务、报告和种子占用事实恢复状态，确保刷新、重复进入、组件重挂载或双击不会重复创建任务、预约资源、扣减权益或生成报告。
- [x] 7.4 补充状态转换、当日已有任务/报告、网络失败重试、离开再进入、刷新、前进后退和重复提交测试。
- [ ] 7.5 删除每日任务域的旧数字步骤与不可达分支，将该阶段作为独立可回滚提交推送到远程仓库。

## 8. 收尾清理、性能与可观测性

- [ ] 8.1 删除 R1 已迁移范围内残留的全局 `view`、`profileSteps` 和数字步骤控制，仅保留有明确移除期限且测试覆盖的必要兼容层。
- [ ] 8.2 清理首页入口对受保护业务域、R1.1 原型和非首屏样式的静态导入，确认构建输出形成可独立缓存的业务域 chunk。
- [ ] 8.3 完成 CSS 去重与域级加载，运行视觉回归、移动端适配和低速网络测试，并修复样式闪烁或布局位移。
- [ ] 8.4 验证所有页面的标题、焦点、键盘操作、加载/错误提示和返回行为，确保路由诊断与错误上报不记录表单、令牌或个人信息。
- [x] 8.5 执行全量构建产物分析，达成首页及业务域预算；若未达标，继续拆分依赖或延迟加载，禁止仅放宽阈值通过验收。

## 9. 全量验收、测试环境发布与回滚验证

- [x] 9.1 运行 lint、类型检查、单元测试、集成测试、静态导出、接口契约检查和性能预算检查，修复所有由本次整改引入的问题。
- [x] 9.2 对全部固定路由执行直接 HTTP 访问验证，确认静态服务器均返回 200 且刷新不回退到首页或 404。
- [ ] 9.3 在真实浏览器执行登录、同意、建档、每日任务和“我的”端到端回归，覆盖刷新、前进后退、失败重试、双击与重复挂载。
- [x] 9.4 部署前先拉取测试服务器最新代码并核对当前发布版本；如有差异先合并验证，再使用不可变前端 release 发布并确认 Nginx 路由回退、gzip 和缓存头。
- [x] 9.5 启动并验证测试环境 API 与 Worker，执行健康检查及核心接口冒烟测试，确认前端迁移未改变接口地址、参数、幂等性或后台任务行为。
- [x] 9.6 记录性能前后对比、路由验收、业务回归和服务健康证据，更新 R1.0 交接/发布文档，并将最终阶段作为独立提交推送到远程仓库。
- [x] 9.7 使用上一不可变 release 完成一次回滚演练；业务验收通过后再移除剩余兼容入口，并保留明确的版本回退步骤。
