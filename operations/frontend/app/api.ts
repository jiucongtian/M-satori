export const apiBase=process.env.NEXT_PUBLIC_OPERATIONS_API_BASE||'/operations-api';
export async function operationsFetch<T>(path:string,init?:RequestInit):Promise<T>{const token=typeof window==='undefined'?'':localStorage.getItem('fresh_operations_token')||'',headers:Record<string,string>={authorization:`Bearer ${token}`,...(init?.headers as Record<string,string>|undefined)};if(init?.body)headers['content-type']='application/json';const response=await fetch(`${apiBase}${path}`,{...init,headers});if(!response.ok)throw new Error((await response.json().catch(()=>null))?.message||'请求失败');return response.json() as Promise<T>}
